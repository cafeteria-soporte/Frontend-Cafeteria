# URL base del backend (incluye el prefijo /api/cafeteria).
# Producción (Railway):
VITE_API_URL=https://cafeteria-server-production.up.railway.app/api/cafeteria
# Local (segun API_PREFIX del backend, por defecto "api"):
# VITE_API_URL=http://localhost:3000/api
